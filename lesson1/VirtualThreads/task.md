Java 24 feature: Synchronize Virtual Threads without Pinning (JEP 491)

Goal: Run a synchronized block in a virtual thread and observe it runs normally (no special flags).

How to run:
- Use JDK 24.

Example in this task:
```java
// See src/Task.java
``` 